{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc build active file",
            "command": "/bin/gcc",
            "args": [
                "-g",
                "${fileDirname}/server.c",
                "${fileDirname}/pub.c",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/bin"
            }
        }
    ],
    "version": "2.0.0"
}